query currentState($token: String!) {
    currentState(token: $token){
        game {
            _id,
            phase,
            time,
            players {
                username
                lastWill
                died
                role
                status
                interactionResults
            },
            createdAt,
            updatedAt
        }
    }
}