query currentState($token: String!) {
    currentState(token: $token){
        game {
            id,
            users{
                username
            },
            createdAt,
            updatedAt
        }
    }
}