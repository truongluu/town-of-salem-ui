query currentState($token: String!) {
    currentState(token: $token){
        game {
            _id,
            players {
                username
                lastWill
                died
                role
            },
            createdAt,
            updatedAt
        }
    }
}