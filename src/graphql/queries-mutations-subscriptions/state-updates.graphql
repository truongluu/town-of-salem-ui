subscription stateUpdates($token: String!) {
    stateUpdates(token: $token){
        lobby {
            id,
            users,
            isClosed,
            updatedAt
        },
        game {
            id,
            users {
                username
            }
            createdAt
            updatedAt
        }
    }
}