subscription stateUpdates($token: String!) {
    stateUpdates(token: $token){
        lobby {
            id,
            users,
            isClosed,
            updatedAt
        },
        game {
            _id,
            players {
                username
                lastWill
                died
                role
            }
            createdAt
            updatedAt
        }
    }
}